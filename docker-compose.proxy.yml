services:
  backend:
    networks:
      - default
      - proxy

  frontend:
    networks:
      - default
      - proxy

networks:
  proxy:
    external: true
    name: ${PROXY_NETWORK:-proxy_default}
